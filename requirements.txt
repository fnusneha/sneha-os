requests>=2.31
python-dotenv>=1.0
google-api-python-client>=2.100
google-auth>=2.23
google-auth-oauthlib>=1.0
polyline>=2.0

# Added for cloud migration (Sneha.OS v2)
psycopg[binary]>=3.1
flask>=3.0
garminconnect>=0.2.17
gunicorn>=22.0
# Ensures zoneinfo works in minimal container images (no /usr/share/zoneinfo
# baked in). tz.py depends on this to get Pacific time right on Render.
tzdata>=2024.1
